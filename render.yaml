services:
  - type: web
    name: lottery-analysis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --port=$PORT --host=0.0.0.0 wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
